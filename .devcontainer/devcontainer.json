{
  "name": "PyTorch CPU DevContainer",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash"
  },
  "extensions": [
    "ms-python.python",
    "ms-toolsai.jupyter"
  ],
  "forwardPorts": [],
  "postCreateCommand": "pip install -r requirements.txt || true",
  "remoteUser": "vscode"
}
